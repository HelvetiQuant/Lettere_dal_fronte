# TODO — 10 Luglio 2026

## Priorita' 0: Bando MiC Grande Guerra 2026/2027 (SCADENZA 15 LUGLIO ORE 12:00)

### Task
- [ ] Registrarsi sul portale https://grandeguerra.cultura.gov.it/presenta-la-tua-domanda/
- [ ] Scaricare e leggere bando completo + vademecum
- [ ] Preparare descrizione progetto (tipologie A + B + E)
- [ ] Compilare cronoprogramma (Allegato B)
- [ ] Preparare dichiarazione sostitutiva (Allegato A)
- [ ] Allegare documentazione fotografica/progetto
- [ ] Inviare candidatura entro 15/07/2026 ore 12:00
- [ ] Valutare partnership con Museo Storico Guerra Rovereto o Archivio di Stato

---

## Priorita' 1: Research-to-Index (nessuna ricerca persa)

### Stato: ✅ IMPLEMENTATO — test su 50 soldati completato

### Completato
- [x] `research_to_index.py` — modulo completo con tabelle e funzioni
- [x] `create_minimal_subject_from_query(query)` — crea record minimo con status=not_verified
- [x] `upsert_source_locator(result)` — salva/aggiorna fonte in fonti_indice (con _safe_str)
- [x] `link_subject_to_source(subject_id, source_id, relation_type, confidence, note)`
- [x] `update_subject_confidence(subject_id)` — ricalcola confidence
- [x] `identify_research_gaps(subject_id)` — identifica campi mancanti
- [x] `enrich_subject_from_sources(subject_id)` — estrae dati strutturati
- [x] `auto_index_if_not_found(query)` — funzione principale
- [x] `index_external_sources_for_soldier(soldier_id)` — test su 50 soldati
- [x] `get_research_stats()` — statistiche
- [x] Filtro rilevanza: relevant vs catalog_ref vs skipped
- [x] Name match: cognome nel titolo/descrizione rilevato
- [x] Test su 50 soldati: 437 fonti, 50 subjects, 1174 link, 200 gaps

### Da fare
- [x] `POST /api/research/query` — endpoint API per auto-index
- [x] `POST /api/research/auto-index` — forza creazione soggetto
- [x] `GET /api/research/subjects/{id}` — dettaglio soggetto con fonti
- [x] `GET /api/research/subjects/{id}/dashboard` — dashboard completa
- [x] `GET /api/research/subjects` — lista soggetti (filtri: tipo, status, confidence)
- [x] `PATCH /api/research/subjects/{id}` — aggiorna status/confidence
- [x] `GET /api/research/gaps` — lista gaps aperti
- [x] Integrazione frontend: `convSearch()` → se no risultati, chiama `/api/research/query`
- [x] Badge "Non verificato" / "Parzialmente verificato" / "Verificato" nei risultati
- [ ] Tab "Gaps" nei risultati investigativi

---

## Priorita' 1bis: Dossier Verificato (Biografie AI) — NUOVO 10/07 sera

### Completato
- [x] `biography.py` — biografia soldato (riusa `soldier_dashboard`) ed evento (riusa `memory_router.route_query`)
- [x] Separazione fonti verificate / da verificare nel prompt (mai inventare da fonti non recuperate)
- [x] Fallback automatico gpt → claude → mistral → perplexity
- [x] Endpoint `POST /api/biography`
- [x] Bottone "Dossier verificato" in UI + card dedicata

### Da fare
- [ ] **Test end-to-end con chiavi API reali** (soldato + evento) — non testabile dal sandbox Cowork, va fatto su questa macchina
- [ ] Verificare che il fallback tra provider funzioni davvero quando una chiave manca (test con `PERPLEXITY_API_KEY` non settata, es.)
- [ ] Valutare se spostare il "Dossier verificato" in un tab dedicato invece che in cima a "Risposta AI" (oggi condividono `aiResponseContainer`)
- [ ] Decidere se/come integrare `import_ocr_lettere/ocr_lettere.db` nello schema principale (oggi solo letto a runtime via match sul cognome, mai fuso in `imi_internati.db`)

---

## Priorita' 2: Provider Federation — miglioramenti

### Completato
- [x] WikiTree provider integrato (20° provider) — API genealogiche gratuite
- [x] TNA Discovery API — implementata (ritorna record reali, ma necessita filtro rilevanza)
- [x] Europeana API — implementata
- [x] Internet Archive API — implementata
- [x] Google Books API — implementata
- [x] Gallica/BNF API — implementata
- [x] HathiTrust API — implementata

### Da fare
- [ ] **Provider Arolsen**: implementare query API reale (attualmente stub)
- [ ] **Provider Bundesarchiv**: implementare query API reale (attualmente stub)
- [ ] **Provider SHD/MDH**: implementare query "Memoire des Hommes" API
- [ ] **Provider Internet Culturale**: implementare query API reale (attualmente stub)
- [ ] **Provider Archivportal-D**: implementare query API reale (attualmente stub)
- [ ] **Provider LAC**: implementare query API reale (attualmente stub)
- [ ] **Gestione errori federation**: timeout, rate limiting, retry con backoff
- [ ] **Cache TTL**: implementare scadenza cache effettiva (controllo expires_at)
- [ ] **IIIF viewer**: integrare viewer IIIF per manifest Antenati/Gallica
- [ ] **WikiTree auth**: per profili privati (trusted list), implementare clientLogin

---

## Priorita' 3: Scraping metadati su fonti riguardanti il DB

### Task
- [ ] **NARA Catalog**: scraping metadati T315 (già 1.153 frame in DB). Estendere ad altri record group WW2 Italy.
- [ ] **Antenati**: scraping metadati registri di stato civile per comuni italiani correlati a soldati IMI nel DB. Solo metadati (nome, data, luogo, URL ARK), non immagini.
- [ ] **CWGC**: verificare cross-match tra caduti_cwgc e internati (stesso cognome+nome+data nascita).
- [ ] **Europeana**: query per argomenti militari italiani WW2, salvare metadati in fonti_indice.
- [ ] **Internet Archive**: query per documenti storici italiani WW2 (diari, memorie, opuscoli).
- [ ] **Gallica/BNF**: query per documenti francesi relativi a internamento in Francia.
- [ ] **Bundesarchiv**: query per documenti tedeschi relativi a IMI (Stalag, Arbeitskommando).
- [ ] **WikiTree**: batch search per soldati IMI con cognomi comuni, cross-match con DB locale

### Approccio
- Usare `federated_search()` con query strutturate (cognomi comuni, reparti, luoghi)
- Registrare risultati in `fonti_indice` via `upsert_source_locator()`
- NON scaricare documenti pesanti
- Prioritizzare provider con API accessibili (NARA, Europeana, Internet Archive, WikiTree)
- Per provider con WAF (Antenati, ABMC): solo metadati via HTML parsing, no download immagini

---

## Priorita' 4: Completamenti e fix

- [ ] **BUG CRITICO ricerca multi-parola**: `search_all()` (`database.py`, usata da `GET /api/search`) fa `LIKE '%intera query%'` su tutta la stringa invece di tokenizzarla per cognome/nome. Cercare "Luigi Gaiaschi" o "Rossi Mario" (entrambi esempi suggeriti in UI!) ritorna 0 risultati anche quando il dato esiste. Verificato con "Gaiaschi Giuseppe fu Luigi" (presente in `caduti_albooro`, introvabile con la query attuale). Fix: split della query in token, match incrociato cognome/nome nelle due direzioni, su `internati`/`decorati`/`caduti_*`/`menzioni`.
- [ ] **Verifica integrita' `imi_internati.db` live** (1.4 GB): `python -c "import sqlite3; print(sqlite3.connect('imi_internati.db').execute('PRAGMA quick_check').fetchone())"` da eseguire direttamente su questa macchina — il mount del sandbox Cowork non riusciva a leggerlo ("database disk image is malformed"), probabile artefatto di sincronizzazione durante scrittura concorrente, da confermare che sia innocuo.
- [ ] **Cercare "Luigi Gaiaschi"** (IMI WW2, documenti primari sul Desktop: Stalag, foglio caratteristico, liberazione 1945) nel DB live: assente in tutte le tabelle del backup del 7/7. Verificare se e' stato aggiunto dopo, altrimenti e' un caso concreto per il flusso "fonti pesanti sul Desktop → collegate al soldato → biografia".
- [ ] **Test endpoint dashboard**: verificare `/api/soldiers/{id}/dashboard` con ID reali del DB
- [ ] **Test ricerca conversazionale**: testare vari tipi di query (nome, nome+luogo, reparto, evento)
- [ ] **Verifica analytics bar**: controllare che tutti i conteggi caricino correttamente
- [ ] **Re-run test Research-to-Index** con filtro rilevanza aggiornato (senza cancellare DB)
- [ ] **TNA query refinement**: aggiungere filtri temporali (WW2) per ridurre rumore
- [ ] **Internet Archive query refinement**: aggiungere filtri per pertinenza militare italiana

---

## Priorita' 5: Documentazione

- [x] Aggiornare `ARCHITETTURA_DB.md` con tabelle research_subjects, research_subject_sources, research_gaps
- [ ] Documentare flusso Research-to-Index in README
- [x] Documentare endpoint API nuovi
- [ ] Diagramma flusso: query → search locale → auto-index → federation → dashboard
- [ ] Aggiornare `CONCORSI_EUROPEI.md` con dettagli bando MiC Grande Guerra

---

## Priorita' 6: Funding

- [ ] **Bando MiC Grande Guerra 2026/2027** — scadenza 15 Luglio 2026 ore 12:00
  - https://grandeguerra.cultura.gov.it/documentazione/
  - Tipologie A (censimento) + B (catalogazione) + E (valorizzazione)
  - ~€400-500k budget, 17 progetti finanziati nel 2024/2025
- [ ] **Horizon Europe Cluster 2** — Culture, Creativity and Inclusive Society
- [ ] **Creative Europe** — Cooperation projects
- [ ] **Digital Europe Programme** — Digital for Cultural Heritage
- [ ] **Europeana Research Grants**
